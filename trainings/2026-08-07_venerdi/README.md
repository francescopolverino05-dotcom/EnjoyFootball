# Friday training — 2026-08-07

```text
trainings/2026-08-07_venerdi/
├── training.json
├── video/      ← FULL SESSION → video/session.mp4
├── clips/      ← tactical clips
└── analysis/   ← analyst reports + session-plan PDF
```

1. Put full session video in `video/session.mp4` (or sync from Vimeo)
2. Add clips under `clips/`
3. Add analysis under `analysis/`
4. Update `training.json` and set `status` to `"published"`
