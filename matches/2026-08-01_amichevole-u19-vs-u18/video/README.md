# Full match video

Put the complete match recording here:

```text
video/Amichevole vs U18.mp4
```

(or `video/match.mp4` for future matches — keep the name in sync with `match.json` → `video.fullMatch`)

MP4s are gitignored — they stay on this Mac for local preview; use Vimeo for the live site.
