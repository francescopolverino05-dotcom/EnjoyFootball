# Full match video

Drop the complete match recording here as:

```text
video/match.mp4
```

`match.json` → `video.fullMatch` points to this file (Full Match tab).

MP4s are gitignored — keep them local for `npm run dev`.
